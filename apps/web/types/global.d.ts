// Ambient module declarations for side-effect asset imports.
// TypeScript 6+ requires explicit declarations for non-TS modules
// imported purely for their side effects (e.g. global stylesheets).

declare module '*.css'
