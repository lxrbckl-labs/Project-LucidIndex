# Keep build context lean — pnpm and Next.js rebuild inside the image.

# Dependencies — rebuilt from lockfile inside image
node_modules
**/node_modules

# Next.js build output — rebuilt inside image
.next
**/.next

# pnpm store cache
.pnpm-store
**/.pnpm-store

# Environment files — never bake secrets into images
.env
.env.local
.env.*
!.env.example

# Git metadata
.git
.github
.gitignore

# Logs
logs
*.log

# OS / editor artifacts
.DS_Store
*.swp
*.swo

# Data directories
data
**/data

# Test / coverage outputs
coverage
test-results
playwright-report

# Leftover lock files that shouldn't override the workspace lockfile
yarn.lock
package-lock.json
