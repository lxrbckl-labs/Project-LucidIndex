{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  // TODO: add project references here as packages gain tsconfig.json files
  // e.g. { "path": "./packages/db" }, { "path": "./packages/shared" }, etc.
  "references": []
}
