{
  "name": "@lucidindex/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test:e2e": "playwright test",
    "postinstall": "playwright install --with-deps chromium || playwright install chromium || true",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@node-rs/argon2": "2.0.2",
    "@playwright/test": "1.59.1",
    "@types/node": "25.6.0"
  }
}
